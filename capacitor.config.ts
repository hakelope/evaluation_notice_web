import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.evaluation.notice',
  appName: 'evaluation-notice-app',
  webDir: 'build'
};

export default config;
